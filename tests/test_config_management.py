# Testes específicos para:
#   ### _get_config_file_path() (mockando user_config_dir).
#   ### _load_extension_map_config() (mockando Path.open, json.load, user_config_dir).
#   ### _save_extension_map_config() (mockando Path.open, json.dump, input para confirmação, user_config_dir).